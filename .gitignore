# Rust
target/
**/*.rs.bk
*.pdb

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# Local agent state
.claude/

# OS
.DS_Store
Thumbs.db

# Model weights — never shipped. Downloaded by the user at runtime from
# Hugging Face (ComfyUI / LM Studio model): the user accepts each model's
# licence. See README.
**/*.safetensors
**/*.syn
**/*.gguf
**/*.onnx
**/*.bin

# makepkg working dirs + built packages (regenerated by cargo build + makepkg)
packaging/pkg/
packaging/src/
**/*.pkg.tar.zst
**/*.pkg.tar.xz
**/*.pkg.tar

# Android build output (sources kept; these are regenerated)
**/local.properties
**/.gradle/
**/app/build/
**/jniLibs/

# Logs
*.log
